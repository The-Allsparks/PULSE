package com.qualcomm.robotcore.hardware;

/** Compile-only stub matching FTC {@code DcMotor}. */
public interface DcMotor extends HardwareDevice {
    int getCurrentPosition();

    boolean isBusy();
}
