package com.qualcomm.robotcore.hardware;

/** Compile-only stub matching FTC {@code HardwareDevice}. */
public interface HardwareDevice {
    String getDeviceName();

    String getConnectionInfo();
}
